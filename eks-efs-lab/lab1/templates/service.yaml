apiVersion: v1
kind: Service
metadata:
  name: {{ include "lab1.fullname" . }}
  labels:
    {{- include "lab1.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lab1.selectorLabels" . | nindent 4 }}
